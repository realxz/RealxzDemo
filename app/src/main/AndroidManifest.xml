<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.realxz.realxzdemo"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".context.RealxzApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".task.RequestSuccessActivity" />
        <activity android:name=".net.NetworkErrorActivity" />
        <activity android:name=".client.ClientRequestErrorActivity" />
        <activity android:name=".server.ServerRequestErrorActivity" />
        <activity android:name=".json.JsonExceptionActivity" />
        <activity android:name=".format.FormatExceptionActivity" />
        <activity android:name=".biz.BusinessFailActivity" />
        <activity android:name=".retry.RetryCaseActivity" />
    </application>

</manifest>